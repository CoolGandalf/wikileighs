---
import '../styles/global.css';
import TopBar from '../components/TopBar.astro';
import Palette from '../components/Palette.astro';

interface Props {
  title: string;
  description?: string;
  activeType?: string;
}

const { title, description = 'WikiLeighs — the personal encyclopedia', activeType } = Astro.props;
const BASE_URL = import.meta.env.BASE_URL;
---
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content={description} />
    <meta name="robots" content="noindex, nofollow, noarchive, nosnippet" />
    <meta name="googlebot" content="noindex, nofollow" />
    <title>{title} — WikiLeighs</title>
  </head>
  <body class="grain">
    <TopBar crumb={activeType} />
    <main class="main-col">
      <slot />
    </main>
    <footer class="page-footer">
      <div>WikiLeighs · rendered from <code>notes/</code> · <a href={`${BASE_URL}about`}>about</a> · <a href={`${BASE_URL}random`}>random</a> · <a href={`${BASE_URL}gaps`}>gaps</a></div>
      <div class="footer-pinned">
        <a href={`${BASE_URL}wiki/reading-list`}>reading list</a> · <a href={`${BASE_URL}wiki/todo`}>todo</a> · <a href={`${BASE_URL}wiki/project-status-dashboard`}>dashboard</a> · <a href={`${BASE_URL}wiki/trends-index`}>trends</a> · <a href={`${BASE_URL}wiki/bookshelf`}>bookshelf</a> · <a href={`${BASE_URL}project-field/`}>foundation</a>
      </div>
    </footer>
    <Palette />
  </body>
</html>
